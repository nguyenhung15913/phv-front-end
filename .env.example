# ─── Pho Huong Viet API — Environment Variables ───────────────
# Copy this file to .env and fill in your values

# Port the API server listens on (default: 3001)
PORT=3001

# The URL of the app that receives order notifications
# e.g. a Make.com webhook, Zapier webhook, your own backend, etc.
WEBHOOK_URL=https://your-app.com/webhook/orders

# Optional: a secret token sent in X-Webhook-Secret header
# so your receiving app can verify requests came from this API
WEBHOOK_SECRET=your_secret_token_here

# Allowed origin for CORS (use * to allow all, or your website URL)
ALLOWED_ORIGIN=https://phohuongviet17.com
